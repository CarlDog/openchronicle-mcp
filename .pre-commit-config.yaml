# Pre-commit configuration for OpenChronicle
# Enforces hexagonal architecture boundaries to prevent regression
#
# Install pre-commit: pip install pre-commit
# Install hooks: pre-commit install
# Run manually: pre-commit run --all-files

repos:
  # Python Code Quality - Ruff (replaces black, isort, flake8)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.8.4
    hooks:
      - id: ruff
        name: Lint with Ruff
        args: [--fix]
        types_or: [python, pyi]
        exclude: ^v1\.reference/
      - id: ruff-format
        name: Format with Ruff
        types_or: [python, pyi]
        exclude: ^v1\.reference/

  # Python Type Checking - Mypy
  - repo: https://github.com/pre-commit/mirrors-mypy
    rev: v1.19.0
    hooks:
      - id: mypy
        name: Type check with Mypy
        args: [src, tests, plugins, --config-file=pyproject.toml]
        additional_dependencies: [pytest]
        pass_filenames: false

  # Markdown Linting
  - repo: https://github.com/DavidAnson/markdownlint-cli2
    rev: v0.18.0
    hooks:
      - id: markdownlint-cli2
        name: Lint Markdown with markdownlint-cli2
        args: [--fix]
        types: [markdown]
        exclude: ^(node_modules|\.venv|v1\.reference)/

  # General File Quality
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v6.0.0
    hooks:
      - id: check-yaml
        name: Check YAML syntax
        exclude: ^v1\.reference/
      - id: check-json
        name: Check JSON syntax
        exclude: ^v1\.reference/
      - id: check-toml
        name: Check TOML syntax
        exclude: ^v1\.reference/
      - id: check-merge-conflict
        name: Check for merge conflicts
      - id: trailing-whitespace
        name: Trim trailing whitespace
        exclude: ^v1\.reference/
      - id: end-of-file-fixer
        name: Fix end of files
        exclude: ^v1\.reference/
      - id: check-added-large-files
        name: Check for large files
        args: [--maxkb=1000]

# CI Configuration
ci:
  autofix_commit_msg: |
    [pre-commit.ci] auto fixes from pre-commit hooks

    for more information, see https://pre-commit.ci
  autofix_prs: true
  autoupdate_branch: ""
  autoupdate_commit_msg: "[pre-commit.ci] pre-commit autoupdate"
  autoupdate_schedule: weekly
  skip: []
  submodules: false
