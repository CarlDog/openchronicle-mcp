# Status

This project's status lives in [`docs/CODEBASE_ASSESSMENT.md`](docs/CODEBASE_ASSESSMENT.md).

This file exists at the root to match the sibling MCP-repo convention
(STATUS.md = "single source of truth for project state"). The actual
content stays under `docs/` because the assessment is long-form and
benefits from sitting next to the rest of the architecture docs.
